version: "3"
services:
  postgresql:
    container_name: pharmacogenomics-postgresql
    image: postgres:15
    env_file:
      - .env.postgresql
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    ports:
      - "54321:5432"
    restart: always
